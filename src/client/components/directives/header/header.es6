'use strict';

app.directive('header', () => {
    return {
        templateUrl: 'header.html',
        scope: {},

        link(scope, element, attrs) {

            var init = () => {
            };

            init();
        }
    }
});
